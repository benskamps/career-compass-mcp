# Pipeline pass — 2026-07-13 (baseline)

First entry. This pass **established** the pipeline (playbook + Routine) rather
than working the loop end-to-end, but it captures a full baseline so the next run
has a starting point and a backlog.

## Health — GREEN

- `npm ci` — ok
- `npm run build:mcp` — ok
- `npm run test:mcp` — **8 files, 80 tests passing** (up from 49 at the
  2026-06-21 night-shift pass; the four scaffold-tool modules + E2E smoke landed
  since then).
- `npm run pack:guard` — not re-run this pass; part of the standard health check
  going forward.

## Security — BACKLOG (for the first scheduled pass)

`npm audit`: **13 advisories** (1 low, 5 moderate, 5 high, 2 critical). All in the
**dev / dashboard toolchain** (transitive under `vite` / `ws`, pulled in by the
Next dashboard + `@modelcontextprotocol/inspector`), not in the shipped MCP
runtime. Notable:

- `ws` (high) — uninitialized memory disclosure + DoS from tiny fragments.
- `vite` (moderate) — `server.fs.deny` bypass on Windows alternate paths.

`npm audit fix` reports a fix is available. **Left for a dedicated PR** on the next
pass so the change is isolated and CI-verified. These do not touch the published
tarball's runtime deps, but should still be cleared.

## Freshness — BACKLOG

`npm outdated` — 15 direct deps behind. Safe patch/minor bumps for a PR:

| Package | Current → Latest | Note |
|---|---|---|
| `@modelcontextprotocol/sdk` | 1.28.0 → 1.29.0 | runtime — bump + retest |
| `next` | 16.2.1 → 16.2.10 | dashboard patch |
| `react` / `react-dom` | 19.2.4 → 19.2.7 | patch |
| `zod` | 4.3.6 → 4.4.3 | runtime — minor |
| `yaml` | 2.8.3 → 2.9.0 | runtime — minor |
| `vitest` | 4.1.2 → 4.1.10 | dev |
| `tsx` | 4.21.0 → 4.23.1 | dev |
| `playwright` | 1.59.1 → 1.61.1 | dev |
| `tailwindcss` / `@tailwindcss/postcss` | 4.2.2 → 4.3.2 | dev |

**Major bumps — human review, do NOT auto-merge:**

- `typescript` 6.0.2 → **7.0.2**
- `@types/node` 25.5.0 → **26.1.1**
- `@modelcontextprotocol/inspector` 0.21.1 → **0.22.0**

## Docs / triage

- Open issues: **0**. Open PRs: **0**. Nothing to triage.
- README spot-check deferred to the first working pass (verify tool/resource
  counts against `listTools`/`listResources`).

## Left for a human

- Nothing blocking. The dep + audit backlog above is the first scheduled pass's
  work; majors are flagged for Ben's call.
