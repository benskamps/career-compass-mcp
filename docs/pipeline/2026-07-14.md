# Pipeline pass — 2026-07-14 (Sprint 1: "Currency & Honesty")

First working pass. Theme: bring the shipped package up to date, and make what
it claims match what it ships. Kicked off right after establishing the pipeline
(see `2026-07-13.md` baseline).

## Published-package health — GREEN (verified from npm)

Clean-room install of `career-compass-mcp@latest` (2.0.0) from the registry, then
a live MCP client driven against the **published** binary:

- `npm i career-compass-mcp@latest` — 117 pkgs, no errors; bin resolves.
- `initialize` handshake OK (316 ms) → `career-compass v2.0.0`.
- 11 tools · 8 resources · 3 prompts · `career://profile` hydrates the example KB
  · `manage_pipeline(stats)` renders · clean shutdown.
- Tarball hygiene: only `data/example/` ships (no `data/career`/`data/pipeline`
  leak); no compiled tests in the tarball. Leak guard holding.

## Security + freshness — DONE (in-range only)

Refreshed the lockfile within existing caret ranges (`npm update` +
`npm audit fix`, no `--force`). Only `package-lock.json` changed — no
`package.json` range bumps, no majors.

- **`npm audit`: 13 → 2.** Was 1 low / 5 moderate / 5 high / 2 critical; now
  2 moderate. Cleared: hono (many), fast-uri, ws, esbuild, ip-address,
  express-rate-limit, and the `next` high advisories — all via in-range
  transitive updates.
- **Remaining 2 (moderate):** `next` → vulnerable `postcss`; only fixable via a
  breaking `--force`. **Deferred** to human review; tied to the dashboard/next
  decision in #16/#17.
- **Direct-dep pins refreshed:** sdk 1.28→1.29, zod 4.3→4.4, yaml 2.8→2.9,
  next 16.2.1→16.2.10, react/react-dom 19.2.4→19.2.7, plus
  vitest/tsx/playwright/tailwind.

Honest scope note: our runtime deps use caret ranges, so consumers already pull
these fixed versions on a fresh install. This lockfile bump mainly hardens **our
CI + contributor installs** and clears the audit noise — not a consumer-facing
security fix. The shipped MCP runtime (`sdk`, `yaml`, `zod`) had **no** advisories
at any point; all noise was in `next` (dashboard) + `inspector` (dev tooling).

Verified green after the bump: `build:mcp`, `test:mcp` (80), `pack:guard` (7),
and a live stdio handshake against the freshly built server.

## Filed for human decision

- **#16** — dashboard absent from the npm tarball (`files:` over-promises).
  Options A (ship it via `prepublishOnly` next build) vs B (drop the globs + make
  the README honest now). Recommended B now, A as follow-up.
- **#17** — major bumps held for review: `typescript` 6→7, `@types/node` 25→26,
  `@modelcontextprotocol/inspector` 0.21→0.22 (the inspector one also clears the
  last audit noise). One PR each, inspector first.

## Left for a human / next pass

- Decide #16 and #17.
- Quality track (not started this sprint): the "career KB that accrues" idea —
  the most in-character next feature. Candidate for the first *quality* pass once
  currency/honesty is settled.

## Changes landed

- `chore(deps): refresh lockfile in-range` (this branch) — clears 11 of 13
  advisories, verified green.
